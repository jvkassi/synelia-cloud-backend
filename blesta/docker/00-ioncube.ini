; ionCube requires its zend_extension to be the very first entry loaded,
; before any other extension -- hence the "00-" prefix so it sorts ahead
; of every other file in conf.d/ (numbered or not). Everything else lives
; in 99-custom.ini.
zend_extension=/opt/ioncube/ioncube_loader_lin.so
